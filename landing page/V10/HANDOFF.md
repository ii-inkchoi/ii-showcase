# HANDOFF — II 랜딩 V10 모바일 리빌드

> 마지막 갱신 2026-08-16. **새 세션은 이 문서 하나로 시작할 수 있어야 한다.**
> 이전 세션 문서: `SESSION-2026-08-13.md` (배경·섹션별 확정 사항이 더 자세함. §5-B 에 정합 내역)
> **V10 은 V9 의 후속 폴더명일 뿐 내용상 새 방향이 아니다.** 아래 §0-B 가 V10 이 생긴 이유.

---

## 0. 30초 요약

Dave (CEO) 가 2026-08-13 회의에서 "새 웹사이트 모바일에 중요한 컨텍스트가 다 빠져 있다"고 했다. V8 모바일은 데스크탑 카피를 **잘라서** 만든 것이었고, 새 방향은 **자르지 말고 재배치**다. 그게 V9 → V10.

- **홈 `V10/index.html`** = 완료
- **SD `V10/self-directed.html`** = **완료** (마무리 3 건 종료)
- **나머지 7 페이지** = `V8/` 에 원본 그대로 대기
- **디자인 시스템** = v15 → **v16** 갱신 완료 (웹 랜딩 섹션만, 앱 무변경)
- **밴드 이미지 높이** = §7-1 C(360) 채택·적용 완료 (2026-08-16)
- **SD 마무리 3 건** = §7-2 전부 완료 (2026-08-16). 1·2 번은 손댈 것이 없었음 — 근거는 §9
- **Managed `V10/managed.html`** = **완료** (2026-08-16, §7-3-a). 미결 1 건 = `.tri` 스탯 블록
- **Pricing `V10/pricing.html`** = **완료** (2026-08-16, §7-3-b). 섹션 리듬 104 만 필요했음
- **Manifesto `V10/manifesto.html`** = **완료** (2026-08-16, §7-3-c). 엔트리 15 개는 40/40 유지
- **Newsroom `V10/newsroom.html`** = **완료** (2026-08-16, §7-3-d). 델타 2 곳뿐
- **Press Release `V10/press-release.html`** = **완료** (2026-08-16, §7-3-e). 델타 3 곳
- **Changelog `V10/changelog-entry.html`** = **완료** (2026-08-16, §7-3-f). PR 과 동일한 델타 3 곳
- **Get the App `V10/get-the-app.html`** = **완료** (2026-08-16, §7-3-g). **규칙 0 개 — 104 미적용이 정답**
- **★ 9/9 전 페이지 완료 (2026-08-16).** 페이지 링크 자립 (`../V8/*.html` 0 개), 남은 `../V8/` 는 에셋 37 개뿐
- **바로 할 일** = §7-4 — Dave 리뷰 → 플래그 결정 → **에셋 V10 복사** → Figma 동기화. 미결 2 건은 아래

## 0-B. V10 이 생긴 이유 — 두 컴퓨터가 갈라졌다 (2026-08-16)

같은 V9 를 **두 대에서 따로 고쳤다.** 폴더가 어느 git 리포에도 올라가 있지 않아 서로를 볼 방법이 없었다.

- **A (Desktop 컴퓨터, 08-15)** — DS 정합 패스 + SD `.rows3` 세로 레저 전환. 근거·날짜가 §6 에 문서화돼 있음
- **B (OneDrive 컴퓨터)** — 모바일 JS 게이트를 리사이즈에도 반응하게 개선. 그 외 `· b5` 타이틀 마커, SD 히로 `line-height:1.5` + 밴드 타이틀 `!important`

**V10 = A 를 기준으로 통째로 채택 + B 의 리사이즈 게이트만 이식.** 버린 것과 이유:

| 버린 것 | 이유 |
|---|---|
| `<title>… · b5` | 캐시버스트용 임시 마커 |
| SD `.hero .hbar{line-height:1.5}` | SD 히로 hbar 는 홈과 달리 **배경 박스가 없다** (`.hbar` 에 `background` 미선언). 박스 없는 텍스트에 박스 티어 1.5 를 주면 헐거워지고, 그 여파로 밴드 타이틀에 `!important` 를 덧대야 했다. ref 의 1.1 로 복귀 |
| SD 밴드 타이틀 `!important` | 위 규칙을 되돌리면 불필요 |

> **재발 방지:** 이 폴더는 `Product Design_work files/Prototype/` 안의 `ii-showcase` 리포 영역에 있지만 **V5 이후 한 번도 커밋된 적이 없다** (마지막 커밋 2026-05-21). 두 컴퓨터를 쓸 거면 이 폴더를 커밋 대상에 넣는 것이 정석. 미결.

---

## 1. 환경 세팅 (새 컴퓨터)

**이 폴더는 git 에 올라가 있지 않다.** 폴더를 통째로 복사해 와야 한다. (`Prototype/` 에 `ii-showcase` 리포가 있으나 랜딩 작업물은 추적 대상이 아니다 — §0-B 참조.)

```bash
cd "Product Design_work files/Prototype/landing page" && python -m http.server 8123
```

Claude Code 에서는 `.claude/launch.json` 의 `ii-landing` 설정이 위와 동일하다.

- 접속: `http://localhost:8123/V10/index.html`
- **`file://` 금지** (JS 게이트와 상대경로가 깨진다)
- 브라우저 캐시가 세다 → 항상 `?v=아무값` 을 붙여 열 것
- DS 보기: 리포 루트 `design-system/index.html` (**v16 반영 완료**)

### 파일 지도

| 경로 | 내용 |
|---|---|
| `V10/index.html` | 홈. **완료** |
| `V10/self-directed.html` | SD. **완료** |
| `V10/managed.html` | Managed. **완료** (`.tri` 스탯 1 건 미결) |
| `V10/pricing.html` | Pricing. **완료** |
| `V10/manifesto.html` | Manifesto. **완료** |
| `V10/newsroom.html` | Newsroom. **완료** |
| `V10/press-release.html` | Press Release. **완료** |
| `V10/changelog-entry.html` | Changelog Entry. **완료** |
| `V10/get-the-app.html` | Get the App. **완료** (모바일 블록은 의도적으로 비어 있음 — §7-3-g) |
| `V10/SESSION-2026-08-13.md` | 원 세션 문서 (배경·섹션별 결정·함정) |
| `V9/` | 갈라지기 전 OneDrive 쪽 사본. **참조용, 편집 금지** |
| `landing page_ref/` | Desktop 컴퓨터에서 가져온 인수 묶음. **참조용, 편집 금지** |
| `V8/II Site V2 - *.html` | 아직 손 안 댄 7 페이지 원본 |
| `V8/DESIGN-RULES.md` | 코드 레벨 규칙 |
| `../../../design-system/index.html` | **DS v16** (리포 루트, 반영 완료) |
| `../../../design-system/index.html.bak-v15-20260814` | DS v15 백업 |
| `landing page_ref/design-system-20260814T215640Z-1-001.zip` | DS 원본 zip (인경 제공) |

### 검증 프로토콜 (매번)

- **모바일 3 폭: 375 / 610 / 850** + **데스크탑 1440 스모크**
- 실측 전 반드시: 전 요소에 `.in` 부여 → **700ms 대기** (아래 §8 함정 1)
- 1440 스모크 시그니처: `.navmenu` flex · `.nav .burger` none · `.loop3 .step` absolute · `.loopfig .sys-legend` absolute · 메모 카드 `.dr-doc` 노출
- 파일 무결성: NUL 0, 파일 끝 `</html>`

---

## 2. 구조 — V9 는 어떻게 만들어졌나

`V10/*.html` = **V8 원본 그대로 복사** (데스크탑 = 바이트 동일) + 파일 끝에 **두 블록만** 추가:

- `<style id="v9-mobile">` — 전부 `@media (max-width:900px)` 안. V8 기존 모바일 블록 **위에** 얹혀 오버라이드
- `<script id="v9-mobile-js">` — `matchMedia(≤900)` 게이트. CSS 로 불가능한 DOM 이동만. 모바일로 로드될 때, **또는 창을 900 아래로 줄여 처음 넘어올 때** 1회 실행 (`applyV9.done` 가드). 클래스/ID 이름은 V9 그대로 둔다 — 셀렉터가 전부 여기에 걸려 있어 개명은 순이익 없는 리스크

에셋·페이지 링크는 `../V8/` 참조. **데스크탑은 절대 건드리지 않는다.**

### 전 페이지 공통 원칙

1. **콘텐츠 누락 0** — 데스크탑 카피 전문이 모바일에 보인다. 접기 허용은 "라벨만으로 의미가 서는 보조 리스트" 뿐
2. **긴 논증은 자르지 않고 비트로 쪼갠다** — 문단 사이에 스탯/이미지를 끼워 리듬을 끊는다
3. **이미지 위 텍스트는 히로에서만** (박스 스테이트먼트). 본문 섹션은 타이틀 = 지면 위 (박스 없음) → 이미지 = 단독 비트 → 콘텐츠
4. **가로 스와이프 금지** — 세로 레저 (48px 번호 컬럼 + 0.667px 헤어라인 행 + `padding:24px 0`). 화면 밖 카드 = 누락
5. **스탯은 Anduril 블록** — 큰 숫자 34/500/1.15/−0.069em → 라벨 15/400/1.4 → 헤어라인
6. **섹션 리듬 = 상/하 104**, 같은 색 인접 섹션에만 `c-900` 헤어라인
7. **데스크탑·카피 무변형.** 카피는 Dave canon — 조정 필요하면 플래그만

---

## 3. ★ 가장 중요한 교훈 — 권위 순서

**이번 세션에서 내가 빠진 함정이고, 반복하기 매우 쉽다.**

DS 의 모바일 사다리는 **2026-07-28** 작성분인데, 그 며칠 뒤 Figma 검증과 인경 확정 주석이 **여러 값을 이미 기각**해 놨다. DS 를 무조건 정답으로 두고 페이지를 "고치면" 검증된 결정을 되돌리게 된다.

> ### 모바일 권위 순서
> **Figma 모바일 마스터 (390) > 페이지 CSS 의 날짜 있는 주석 > DS 모바일 사다리**
>
> **DS 값과 코드 주석이 다르면, 페이지를 고치기 전에 주석의 날짜부터 확인할 것.**

실제 증거 (전부 `V10/index.html`):

| 줄 | 주석 |
|---|---|
| `:518` | `.hero .hbar` — **"27 at 390 — confirmed by the Figma mobile master (2026-07-31); the doc's 32 note is stale"** |
| `:733` | **"mono collapses to three values: 13 section label + CTA · 11 meta/date/notice · 9 figure meta (2026-07-28, Inkyung)"** |
| `:743` | 히로 트러스트 라인 **"Settled at 11px per the Figma mobile master (2026-07-31)"** |
| `:592` | `.step .t` 22 의 근거 — **"swipe cards are their own units → 22 stream-heading tier (2026-07-28, Inkyung)"** |
| `:863` | **"mono meta text leads at 1.4, not 1.5 (Inkyung 2026-08-01, all pages)"** — 사유까지 명시 |

이 원칙은 **DS v16 웹 랜딩 섹션 맨 위에도 박아 뒀다.**

---

## 4. 디자인 시스템 — v15 에서 무엇이 바뀌었나

**파일:** `design-system-extracted/design-system/index.html` · **백업:** 같은 폴더 `index.html.bak-20260814`

### 범위 (중요)

DS 는 **앱 스케일**과 **웹 스케일**이 분리돼 있다.

- **앱**: `Color · *`, `Spacing`, `Typography · Headings / Typing Text / Body / Link·Button·CTA / Numbers ($)` (1–381 행)
- **웹**: `Typography · Landing (Web)` (382–465 행) — 자체 모바일 티어/스페이싱 사다리를 가짐
- 웹 섹션이 명시: *"Do not mix this with the product scale"*

**이번 변경은 웹 랜딩 섹션(385–460 행) + 푸터뿐이다. 1–381 행(앱)은 바이트 동일 — 검증 완료.**

> ⚠️ **앱 `Spacing` 스케일(2/4/8/12/16/20/24/32/40/48/**80**)을 웹에 끌어다 쓰지 말 것.** 내가 이번에 80 을 "72 와 104 사이 절충안"으로 잘못 제시했다가 정정했다. 80 은 랜딩 값이 아니다. 웹 모바일 간격은 아래 사다리가 전부다.

### 값 변경 7 건

| 항목 | v15 | **v16** | 근거 |
|---|---|---|---|
| 히로 디스플레이 | 32 | **29 at 390** | Figma 07-31 이 27 확인 + V9 08-13 "scale + air" |
| 섹션 마스트헤드 | 27 | **29 at 390** | 위와 동일 (히로와 한 clamp 공유) |
| 스탯 숫자 | item title 17 에 포함 | **34 / 500 / −0.069em 독립 티어** | Anduril 블록 (08-13). 34 는 원래 DS 공용 사이즈라 새 숫자가 아니라 역할 재배정 |
| 항목 제목 | 17 (스탯 포함) | **17 / 500 / tracking 0** (스탯 분리) | 명확화 |
| 섹션 라벨(아이브로) | 11 | **13** (CTA 와 같은 티어) | 인경 07-28 확정 |
| 모노 행간 | 전부 1.5 | **라벨·CTA 1.5 / meta 1.4** | 인경 08-01 전 페이지 결정 |
| 섹션 패딩 | 72 / 72 | **104 / 104** | 08-13 3폭 실측 |
| 히로 컴플라이언스 | `max(12px,12u)` | **11px c-400** | Figma 07-31 |

### 규칙 3 개 추가

1. **권위 순서** (§3) — 섹션 맨 위
2. **디스플레이 상한 규칙** — 크기는 취향이 아니라 **가장 긴 안 깨지는 줄**에 막힌다. 실측 포함 (아래 §5)
3. **앱 스케일 차용 금지** + 104 채택 사유

### 검증

HTML 파싱 에러 0 · `<div>` 240/240 균형 · 브라우저에서 사다리 9행 정상 렌더 · 앱 섹션 바이트 동일

---

## 5. 히로 크기가 왜 29 인가 (재론 방지)

레퍼런스(GM 35 / Anduril 34)가 더 크니 올리자는 생각이 계속 든다. **물리적으로 불가능하다.**

375 에서 가용 폭 **335px**. `"Most investors do not."` 한 줄의 실제 렌더 폭:

| 크기 | 28 | 30 | **32** | 34 | 35 |
|---|---|---|---|---|---|
| 폭 | 301 | 321 | **342** ✗ | 362 ✗ | 372 ✗ |

**30 이 천장.** 32(구 DS)부터 줄이 깨지고, V9 히로는 검은 클론 박스라 깨지면 박스가 문구 중간에서 갈라진다.

> **더 큰 무게를 원하면 레버는 폰트 크기가 아니라 박스다.** GM·Anduril 이 34~35 로 갈 수 있는 이유는 박스 없는 맨 텍스트라 자유롭게 줄바꿈되기 때문. 박스를 걷어내는 건 브랜드 시그니처 변경이라 별도 결정 사안.

---

## 6. 이번 세션에 적용한 것 (2026-08-14 ~ 15)

### 6-1. DS 정합 드리프트 교정 — `V10/index.html`

DS 가 **이미 금지하고 있던** 값들. 결정할 게 없어 그냥 고친 것.

| 대상 | 이전 | 이후 | 근거 |
|---|---|---|---|
| `.pricehd`·`.pricehd9`·`.lnote`·`.dlead` | 17/500/**1.3** | 15/500/1.4 | 1.3 은 DS 행간(1.1/1.15/1.4/1.5)에 없음. DS 가 "데스크탑 1.3 이 새어내려오는 반복 결함"으로 명시 |
| `.latest .lt-hd` | 1.3 | 1.15 | DS item title |
| `.loop3 .step .d` | 15 | 13 | DS small description 이 "Step descriptions" 명시. 데스크탑도 이미 13 |
| `.loopfig .k` | **12px**/1.6 | 13/1.4 | 12 는 web 에서 폐기 (앱 전용). `.loopfig` 은 replica 면제 목록 아님 |
| `.clog .ci .ct` | 1.15 | 1.4 | DS sub-heading 15/500 = 1.4 |
| `.sys .modes` | flex `gap:48` | `row-gap:0` + `.mode+.mode{margin-top:48}` | DS: "간격은 margin 만, 세로 flex/grid 는 row-gap:0" — DS 가 이 페이지들의 최다 결함으로 지목 |
| `.loopfig .sys-legend` | `gap:14` | `row-gap:0` + `>*+*{margin-top:16}`, 하단 패딩 14→16 | 14 는 사다리 밖 |
| `.loopfig .k` gap | 7 | 8 | 사다리 밖 |

**SD 에도 1 건:** `.grp.pay .lead` 17/500/1.3 → 15/500/1.4

### 6-2. 항목 제목 22 → 17 — `V10/index.html`

`.sys .mode h3` (Managed / Self-Directed / Fiscal.ai Max) 와 `.loop3 .step .t` (Record/Benchmark/Review/Calibrate) → **17/500/1.15/ls 0**

근거 3 중:
1. DS item title = 17
2. **V8 모바일이 이미 `.sys .mode h3` 를 17/1.15/ls 0 으로 두고 있었음** (`index.html:569`) — V9 가 22 로 올린 게 예외
3. 두 레퍼런스가 같은 역할에 GM 17.5 / Anduril 16

`.step .t` 의 22 는 "스와이프 카드는 독립 단위"가 이유였는데 **V9 가 그 스트립을 세로 레저로 바꿨으므로 전제가 소멸.**

### 6-3. SD 가로 스트립 → 세로 레저 — `V10/self-directed.html`

`close9` 섹션 `.rows3` 가 `overflow-x:auto` + `scroll-snap` + 70% 폭 카드 3 개였다. 375 에서 **02 는 x=279–513, 03 은 x=537–772 로 화면 밖** — 독트린 규칙 4 정면 위반이자 실제 콘텐츠 누락. 홈 `.loop3 .lstrip` 규칙 이식 (48px 번호 컬럼 + 헤어라인 행, 제목 17 / 설명 13).

검증: 375·610 에서 3 행 전부 화면 안(left 20 / right 355) · `scrollWidth == clientWidth` · 페이지 오버플로 0 · 1440 데스크탑 `.rows3` grid/`1fr`/`249.2u 1fr` 그대로

**→ 이로써 화면 밖에 숨은 콘텐츠는 전 페이지에서 사라졌다.**

### 6-4. DS v15 → v16 (§4)

### 손대지 않은 것 (의도적)

- **모노 meta 행간 1.4** — 인경 08-01 전 페이지 결정. DS 를 1.4 로 맞췄지 페이지를 1.5 로 바꾸지 않음
- **섹션 아이브로 13** — 인경 07-28 확정. DS 를 13 으로 맞춤
- **섹션 패딩 104 / 스탯 34** — 유지. DS 를 104/34 로 맞춤
- `.close9 .bigh2` 컨테이너 `font-weight:400` — 렌더되는 건 자식 `.a`/`.b` span 이고 둘 다 이미 500. 실렌더 영향 0

---

## 7. 다음 할 일

### 7-1. 밴드 이미지 높이 — ✅ **C 채택, 적용 완료 (2026-08-16)**

`clamp(240px,55vw,340px)` → **`clamp(360px,82.5vw,510px)`** · index 2 곳 (`.v9band>img,.v9band>video` · `.gap2 .v9band>.ph1`) + SD 1 곳 (`.v9band>img`)

**곡선 전체를 1.5 배 했다** (240→360, 55→82.5vw, 340→510). 그래서 **꺾이는 폭이 그대로다** — 436 까지 하한이 지배, 618 부터 상한. 값만 커지고 규칙 모양은 안 바뀐다.

실측 결과:

| 폭 | 높이 | 비율 | 화면 점유 |
|---|---|---|---|
| 375 | **360** | **1.04** | **44%** (GM 46%) |
| 610 | 503 | 1.21 | 56% |
| 850 | 510 | 1.64 | 57% (Anduril 58%) |

375 페이지 길이 11,617 = 아래 표의 C 예측(11,254 + 360)과 일치. index·SD 세 밴드 전부 동일값. 데스크탑 1440 무영향 (`.v9band` 0, 시그니처 전부 유지).

> 넓은 폭(610~850)은 상한 510 에 걸려 비율이 다시 가로형으로 돌아간다. **의도된 것** — 화면이 넓어지면 가로형이 자연스럽고, 점유율은 오히려 Anduril 과 같은 57% 다. 여기서 세로형을 유지하려면 상한을 더 올려야 하는데 그건 별도 판단.

<details><summary>결정 근거 (원본 기록)</summary>

당시 `clamp(240px,55vw,340px)` = 375 에서 240, 비율 1.56 **가로형**. 폰에서 배너/구분선처럼 읽혔다. GM·Anduril 은 둘 다 세로형. **DS 가 규정하지 않는 영역이라 순수 판단 사안이었다.**

375 에서 실측한 후보 (JS 임시 주입으로 비교만 했고 **파일에는 미반영**):

| | 높이 | 비율 | 화면 점유 | 페이지 길이 |
|---|---|---|---|---|
| **A** 현행 | 240 | 1.56 가로형 | 30% | 11,254 |
| **B** | 300 | 1.25 | 37% | +180 |
| **C** ★추천 | 360 | 1.04 정사각 | **44%** | +360 |
| **D** | 420 | 0.89 세로형 | 52% | +540 |

참고: GM 375 높이 = 화면의 46%, Anduril 469 = 58%

**C 추천 이유:** 점유 44% 가 GM 의 46% 와 사실상 같고, 비율 1.04 는 "배너"에서 확실히 벗어나면서 D 처럼 스크롤을 크게 늘리지 않는다. 페이지는 3% 만 길어진다.

적용 위치: `index.html` 의 `.v9band>img,.v9band>video` + `.gap2 .v9band>.ph1`, `self-directed.html` 의 대응 규칙. **index·SD 동시에.**

</details>

### 7-2. SD 마무리 3 건 — ✅ **전부 완료 (2026-08-16)**

1. **research 섹션 하단 잔여 마진** — **수정할 것이 없었다.** 375/610/850 세 폭에서 `section.band` **9 개 전부** 하단 잔여 = 선언된 `padding-bottom` 과 일치 (104), 마지막 자식 `margin-bottom` 0. §6-1 의 `row-gap:0` + margin 전환과 `.band>*:last-child{margin-bottom:0!important}` (`self-directed.html:773`) 이 이미 해결한 상태. **재조사 금지 — 실측 근거는 §9.**
2. **1440 데스크탑 스모크** — 통과. `.v9band` 0 · `.navmenu` flex · `.burger` none · `.rows3` grid(row) · `.rows3 .row` static · 가로 스크롤 0 · 콘솔 에러 0
3. **홈↔SD 링크 V10 연결** — 완료. `index.html` 의 `../V8/II Site V2 - Self-Directed.html` **6 개** (본문 4 + `#research` + `#benchmark`) → `self-directed.html` 로 교체. 브라우저에서 6 개 전부 `/V10/self-directed.html` 로 해석됨을 확인, 앵커 2 개 실재 확인. SD → 홈 3 개는 원래부터 로컬.

> **남은 V8 링크 = 22 개** (Changelog 4 · GetTheApp 4 · Manifesto 4 · Managed 3 · Newsroom 2+2 · Press Release 2 · Pricing 2) + 에셋 13 개. **미빌드 페이지라 정상.** §7-3 에서 한 페이지 완성할 때마다 그 페이지 링크만 V10 으로 돌린다. 에셋은 §7-4 에서 일괄 복사.

### 7-3-a. Managed — ✅ **완료 (2026-08-16)** · `V10/managed.html`

콘텐츠 손실은 거의 없었다 (데스크탑 1,534 단어 vs 모바일 1,494). 실제 작업은 **밴드 문법 포팅**이었다.

| 항목 | 이전 (V8, 2026-08-01) | V10 |
|---|---|---|
| 사진 밴드 `#fee`·`#refuses` | 아이브로 + **박스 마스트헤드가 사진 위에** 절대배치 (top 72 / 115), 배경 268px | **`.v9band` 포팅** — 타이틀이 이미지 **앞**, 박스 제거, 이미지 = 단독 비트 |
| 밴드 높이 | 268 (비율 1.40) | `clamp(360px,82.5vw,510px)` — 375 에서 360 / 1.04 |
| 섹션 패딩 | 72 / 72 | **104 / 104** |
| 히로 아이브로 뒷줄 | 모바일에서 제거 (V8 이 스스로 "Dave flag" 달아둠) | **복원** |

밴드 리듬 실측 (375): 섹션top→아이브로 **104** · 아이브로→타이틀 **24** · 타이틀→이미지 **40** · 이미지→콘텐츠 **64**. 홈·SD 와 동일.

**손대지 않은 것 (의도적):**
- **`.tri` 스탯 블록** — V9 캐논은 Anduril 34/500/−0.069em 이지만, 이 페이지의 `.tri` 는 **인경 2026-08-01 결정**이 촘촘히 달려 있다 (점선 c-600 헤어라인 유지 = "solid 버전 시도했다가 되돌림", 모든 간격 16). 08-13 캐논이 더 나중이라 권위 순서상 이기지만, 명시적으로 되돌린 실험을 재도입하는 셈이라 **인경 판단 필요.** ⬅ 미결
- `#track-record` 의 `table.tbl.wide` — 660px, `.tblwrap` 안에서 자체 가로 스크롤 (래퍼 20~355, 페이지 가로 스크롤 0). 표의 가로 스크롤은 모바일 캐논이 허용하는 예외라 유지. 판단이 필요하면 플래그.
- 바텀시트 (5 슬라이드 + `.sdots` 인디케이터 + 닫기) — 정상 동작 확인, 무변경
- 카피·데스크탑 전부

**데스크탑 무변경 증명:** 추가 블록을 떼고 경로를 원복해 V8 원본과 대조 → **실내용 1,141 줄 완전 일치.**

**검수:** 375 / 610 / 850 각 폭에서 자체 스크롤 컨테이너(`.tblwrap`·`.sheet`) 제외 시 화면 밖 요소 **0**, 페이지 가로 스크롤 0. 모바일 1.3 행간 잔재 **0**. 1440 스모크: `.v9band` 0 · navmenu flex · burger none · 표 노출 · pflist 숨김 · `.bg` absolute 풀높이. 검증기 **0 error / 0 warning**. 무결성 NUL 0 · div 97/97 · script 5/5 · JS 문법 OK.

### 7-3-b. Pricing — ✅ **완료 (2026-08-16)** · `V10/pricing.html`

**거의 손댈 것이 없었다.** 콘텐츠 손실 0 (데스크탑 890 단어 vs 모바일 879 — 차이는 내브 리스트와 언어 스위처뿐, 둘 다 버거 오버레이 안에 있다). 가로 스트립·사진 밴드·표·`line-height:1.3` 잔재 전부 **없음**. 타입/간격은 07-31 · 08-01 패스가 이미 사다리에 올려놨다.

> **이 페이지는 어휘가 다르다.** `.p-hero` / `.p-sec` / `.p-block` / `.p-item` / `.p-qa`, 리빌 클래스는 `.rv` 가 아니라 **`.ri`**, 크림 계열. `band` / `rail` / `secpad` 가족이 아니라서 **`.v9band` 를 이식할 대상이 없고, 옮길 DOM 도 없어 동반 스크립트를 넣지 않았다.** Manifesto 도 같은 가족일 가능성이 높으니 착수 전 클래스 어휘부터 확인할 것.

유일한 V10 델타 = **섹션 리듬 72 → 104** (`.p-sec` · `.p-sec:first-of-type` · `.p-hero` 하단 · `.bottom-canon`). 히로 상단 120 은 유지 — 고정 내브를 비우는 값이지 섹션 리듬이 아니다.

**검수:** 375 / 610 / 850 화면 밖 0 · 페이지 가로 스크롤 0 · `.p-sec`·`.bottom-canon` 하단 잔여가 패딩과 일치(불일치 0) · 1440 스모크 정상(navmenu flex · burger none · 데스크탑 패딩 124.5u 유지 · 890 단어) · **데스크탑 V8 과 실내용 367 줄 완전 일치** · 검증기 0 error.

### 7-3-c. Manifesto — ✅ **완료 (2026-08-16)** · `V10/manifesto.html`

Pricing 과 같은 크림 가족 (`.ri` 리빌, `.m-hero` / `.m-entry` / `.m-head` / `.m-name` / `.m-body`). 이미지·표·가로 스트립 없음 → `.v9band` 이식 대상 없음, 스크립트 없음. 콘텐츠 손실 사실상 0 (데스크탑 1,414 단어 vs 모바일 1,400).

**★ 15 개 독트린 엔트리는 40/40 을 유지했다 — 104 를 밀지 않았다.**

V8 이 사유를 남겨 뒀다: *"doctrine entries - 16 of them, so 40/40 rather than the 72 used on the homepage"*. 엔트리는 **섹션이 아니라 스트림 안의 병렬 항목**이고, V10 사다리에서 병렬 항목은 48 이지 104 가 아니다. 전부 104 로 밀면 페이지가 8,668 → 약 10,600px (**+22%**) 가 된다. 실제로 104 를 적용한 건 아래 4 곳뿐:

| 대상 | 이전 | V10 |
|---|---|---|
| `.m-hero` 하단 | 72 | 104 (상단 120 은 고정 내브 클리어런스, 유지) |
| `.m-entry:first-child` 상단 | 72 | 104 |
| `.bottom-canon` | 72 / 72 | 104 / 104 |
| `.m-legal` | 48 / 72 | 104 / 104 |

**히로 아이브로 복원:** V8 이 모바일에서 `INTELLIGENT INVESTING &middot;` 를 숨기고 있었다 (사유: "same call as the SD/Managed hero eyebrows" — 그 두 페이지는 V10 에서 이미 되돌렸다). inline 으로 복원해 한 줄로 읽힌다. **실측 308 / 335px, 여유 27px, 1 줄.**

> ⚠️ **`.m-deck` 의 `<br class="dtonly">` 는 그대로 숨겨 뒀다.** 같은 `dtonly` 클래스지만 저건 카피가 아니라 데스크탑 측정용 줄바꿈이라, 복원하면 문장 중간이 갈라진다. 셀렉터를 `.m-hero .m-eyebrow span.dtonly` 로 좁힌 이유.
>
> 판단 여지: 복원한 문구는 **브랜드명 반복**이라 바로 위 내브 워드마크와 중복된다 (SD/Managed 에서 되돌린 "PART OF THE INTELLIGENT INVESTING SYSTEM" 은 고유 카피였다는 점에서 성격이 다르다). 되돌리려면 위 한 줄만 지우면 된다.

**검수:** 375 / 610 / 850 화면 밖 0 · 텍스트 넘침 0 · 가로 스크롤 0 · 1440 스모크 정상 (첫 엔트리 124.5u / 나머지 62.2u = V8 원설계 유지) · **데스크탑 V8 과 실내용 386 줄 완전 일치** · 검증기 0 error.

### 7-3-d. Newsroom — ✅ **완료 (2026-08-16)** · `V10/newsroom.html`

지금까지 중 가장 단순했다. 크림 가족 (`.ri`, `.nr-hero` / `.nr-list` / `.nr-close`), 3 섹션 + 4 행. 이미지·표·가로 스트립·`dtonly` 전부 없음 → 이식할 문법도, 옮길 DOM 도 없음 (스크립트 없음). 콘텐츠 손실 0 (582 vs 571, 차이는 내브·언어 스위처).

**§7-3 의 티어 경고는 여기서 물지 않았다.** `.nr-list .row .t` 가 이미 **17/1.15** 항목 제목 티어이고, 22 로 올라간 곳이 없다. 이 페이지엔 별도 스트림 오프너가 없다 — `.nr-tabs` 필터 행이 그 역할을 한다.

델타 2 곳뿐: `.nr-list` 상단 72 → **104**, `.nr-close` 72/72 → **104/104**. `.nr-hero` 는 120/**0** 유지 — 상단은 고정 내브 클리어런스이고 하단 0 은 의도된 값 (`.nr-tabs` 의 `margin-top:40` 이 리스트까지 간격을 넘긴다).

> **확인하고 그냥 둔 것:** `.nr-tabs` 의 `scrollWidth` 가 `clientWidth` 보다 6px 크다. 실측하면 버튼 4 개가 한 줄에 다 들어간다 (마지막 `IN THE PRESS` 오른쪽 353, 컨테이너 20~355). 초과분은 탭 타겟 `::after` 오버행이지 잘린 콘텐츠가 아니다. **재조사 금지.**

**검수:** 375 / 610 / 850 화면 밖 0 · 가로 스크롤 0 · **탭 필터 실동작 확인** (ALL 4 행 → PRESS RELEASE 1 행 → ALL 복귀 4 행) · 1440 스모크 정상 · **데스크탑 V8 과 실내용 337 줄 완전 일치** · 검증기 0 error.

### 7-3-e. Press Release — ✅ **완료 (2026-08-16)** · `V10/press-release.html`

크림 가족, 9 섹션, 2,211 단어의 긴 문서 페이지. 가로 스트립·표·`dtonly` 없음, 콘텐츠 손실 0.

**§7-3 의 두 티어 경고가 실제로 적용되는 페이지이고, 둘 다 이미 올바르다:** 스트림 오프너 `.pr-h2` = **22/1.15**, 항목 `.pr-more .li .t` = **13/1.4** small copy. **리스트 항목을 올리지 말 것.**

이미지 1 장(`press-release-light-clean.png`)은 `<figure class="pr-fig">` + `<figcaption>` 의 **문서 도판** (375 에서 335×377). 풀블리드 섹션 사진이 아니라 `.v9band` 문법 대상이 아니다.

델타 3 곳: `.pr-hero` 하단 72 → **104** · `.pr-doc` 상단 72 → **104** · `.pr-close` 72/72 → **104/104**. `.pr-block` / `.pr-legal` / `.pr-more` 의 `margin-top:64` 는 **사다리의 레이어 브레이크**라 유지 (릴리스가 하나의 연속 문서로 읽혀야 한다). 첫 `.pr-block` 의 `margin-top:0` 은 인라인 스타일이라 그대로.

**검수:** 375 / 610 / 850 화면 밖 0 · 가로 스크롤 0 · 도판 로드 OK · 1440 스모크 정상 · **데스크탑 V8 과 실내용 420 줄 완전 일치** · 검증기 0 error.

> 참고 (범위 밖): 도판 원본이 **5552×6243 PNG** 다. §7-4 에셋 복사 때 리사이즈 후보.

### 7-3-f. Changelog Entry — ✅ **완료 (2026-08-16)** · `V10/changelog-entry.html`

**Press Release 와 같은 클래스 체계**(`.pr-hero` / `.pr-doc` / `.pr-block` / `.pr-legal` / `.pr-more` / `.pr-close`) + 체인지로그 전용 `.cl-item` / `.cl-name` 3 개. 613 단어. 가로 스트립·표·`dtonly` 없음, 콘텐츠 손실 0. 그래서 **델타가 Press Release 와 동일한 3 곳**: `.pr-hero` 하단 · `.pr-doc` 상단 · `.pr-close` → 104. `.pr-block` 의 64 는 유지.

**티어 (§7-3 경고):** 이 페이지엔 `.pr-h2` 가 없다. `.cl-name` (**22/1.15**, 데스크탑도 22) 이 릴리스 항목을 이고, `.pr-more .li .t` 는 13. 두 단이 다 살아 있어 고칠 게 없었다. **`.cl-name` 을 17 항목 티어로 "정규화"하지 말 것** — 히로 아래 이 페이지의 유일한 헤딩이다.

**검수:** 375 / 610 / 850 화면 밖 0 · 가로 스크롤 0 · 1440 스모크 정상 · **데스크탑 V8 과 실내용 352 줄 완전 일치** · 검증기 0 error.

### 7-3-g. Get the App — ✅ **완료 (2026-08-16)** · `V10/get-the-app.html` · **규칙 0 개**

**손댈 것이 없었다.** 섹션 1 개짜리 전체화면 페이지 (`.gta-section` = `min-height:100vh` + flex 세로 중앙정렬). 375 실측: 섹션 높이 812 = 뷰포트 812, 442 단어, 콘텐츠 손실 0, 배지 이미지 로드 OK. 타입·간격 전부 사다리 위 (아이브로 13 모노 · 타이틀 `clamp(26,6.92vw,40)` · 덱 15/1.4 · 노트 13/1.4 · sign-in 룰 margin 48 + padding 48).

> **★ 104 를 적용하지 않았고 그게 맞다.** `.gta-section` 은 `padding:120px 20px 72px` 유지. 다른 여덟 페이지에서 104 는 **쌓인 섹션 사이의 간격**을 뜻한다. 이 페이지엔 두 번째 섹션이 없어서 저 패딩은 중앙정렬 콘텐츠 주변의 화면 여백일 뿐이고, 104 로 올리면 가운데 블록이 눌리기만 한다. (다른 페이지의 히로 상단 120 을 그대로 둔 것과 같은 논리 — 내브 클리어런스이지 리듬이 아니다.)
>
> 파일에 **빈 `<style id="v9-mobile">` 블록 + 이 사유를 주석으로** 넣어 뒀다. 나중에 "이 페이지만 왜 안 건드렸지" 하고 다시 파지 않도록.

### 7-3-h. 전 페이지 완료 — 링크 자립 + 통합 검수 (2026-08-16)

**`../V8/*.html` 페이지 링크 0 개.** 사이트가 V10 폴더 안에서 완전히 자립한다. 남은 `../V8/` 참조 37 개는 전부 **에셋** (이미지 14 · 데스크탑 영상 6 · 모바일 영상 6 · 로고/스크립트 11) — §7-4 에서 일괄 복사 대상.

| 검사 | 결과 |
|---|---|
| 검증기 (9 파일) | **0 error** / 23 warning (전부 `.dr3-*` 리플리카, 면제) |
| 무결성 | 9 파일 NUL 0 · `</html>` · `<div>`·`<style>`·`<script>` 전부 짝 맞음 |
| 링크 크롤 | 상대 참조 **219 개 검사, 깨짐 0** |
| 데스크탑 무변경 | Managed·Pricing·Manifesto·Newsroom·PR·Changelog·GetTheApp = V8 과 실내용 **완전 일치**. index·SD 는 ref V9 기준으로 **밴드 높이 + JS 게이트 + SD 링크 6 개 외 차이 0** |

> ⚠️ **`newsroom.html#press-release` / `#changelog` 는 앵커가 아니다.** Newsroom 의 JS 가 `location.hash` 를 읽어 **필터 탭을 선택**한다 (`select((location.hash||'').replace('#','')||'all')`). id 로 존재하지 않는 게 정상. 실동작 확인함: `#changelog` 로 진입 → CHANGELOG 탭 활성, 4 행 중 2 행 표시. **"깨진 앵커"로 오판하지 말 것.**

### 7-3-i. 전 페이지 통일 스윕 (2026-08-16 오후, 인경 지시 3건)

**① 검정-검정 섹션 헤어라인 (모바일만).** 홈에만 있던 `c-900` 상단선 규칙을 전수 조사 — 빠진 곳 3곳 발견·추가: 홈 `standard→std5b`, Managed `default→#fee` (인경이 캡처로 지적한 그 간격), SD `calibration→#development`. 색이 바뀌는 경계는 독트린대로 선 없음. 크림 페이지의 무선 경계는 데스크탑과 동일한 원설계라 미변경.

**② 밴드 이미지 상하 헤어라인 (다크 3페이지).** 야간 사진의 검은 가장자리가 바탕에 녹아 "타이틀-이미지 간격이 끝없어 보이는" 문제 — `.v9band` 이미지에 온블랙 표준선 `rgba(255,255,255,0.18)` 상/하만 추가 (좌우는 풀블리드 유지). 인경의 원제안은 "이미지 확장(크롭 조정)"이었으나 사진별 판단이 필요해 시스템적 해법(가장자리 선)을 먼저 적용 — 크롭 조정은 사진별로 별도 판단 가능.

**③ 타입 전수 통일 (행간·자간·크기, 375 실측).** 최종 상태:

| 티어 | 9페이지 통일값 |
|---|---|
| 히로/마스트헤드 디스플레이 | **28/1.10/-0.56px(=-0.02em)/500** — DS v16 clamp `28px,7.4vw,40px` (=29@390). 홈 히로만 1.50 = 박스 클론 시그니처 (의도) |
| 스트림 오프너 (PR·Changelog `.pr-h2`/`.cl-name`) | 22/1.15 (별도 티어, 정상) |
| 항목 제목 | 17/1.15/ls0/500 |
| 바디 | 15/1.40/ls0/400 — **원래부터 9페이지 완전 통일이었음** |
| 스몰 | 13/1.40 |
| 모노 라벨 (아이브로) | 13/1.50/0.04em · PR/Changelog 크럼은 11 (07-31 날짜 있는 결정, 유지) |
| 모노 메타 | 1.40 (figcap 9 · 메타 11) |

고친 회귀들:
- **Managed·SD 밴드 타이틀 42px 피치** — V8 박스 규칙(`#fee .bigh2 .a{line-height:1.5}`, ID 특정성)이 이기고 있었음. `!important` 복원 (로컬 V9 에 있던 것이 ref 병합 때 사라진 회귀). SD 는 `::after '\A'` 개행도 잔존 → 제거
- **구 clamp(26,6.92vw) 잔존 7페이지** → v16 clamp 로 (Managed 는 히로가 `.hbar` 라 별도 선언 필요했음)
- **자간 -0.52px 상속 버그** — letter-spacing 은 계산된 px 로 상속되므로 부모(.bigh2=26) 기준값이 28px 스팬에 따라옴. 부모에 v16 clamp + em 자간 선언으로 해결
- **모노 메타 1.5 잔존** — Manifesto `.m-no`, Newsroom `.row .k`, PR/Changelog `.pr-dateline` → 1.4 (인경 08-01 전 페이지 결정)
- **GTA 히로 w400 → 500** (사이트 유일 400). ⚠ 데스크탑 `.gta-title` 은 아직 400 (V8 원본 유지) — 데스크탑 스윕 때 결정

측정 함정 (재발 방지): ① 컨테이너 무게 재지 말 것 — Managed h2 가 w700 로 잡혔지만 전부 스팬(.a=500)이 렌더 (함정 #7). ② Range.getClientRects 로 줄피치 잴 때 스팬 중첩 rect 가 허수 줄을 만든다 — computed font/lh 로 교차 검증.

전 수정 모바일 블록(≤900) 안. 검증기 0 error · 375 화면밖 0 · 데스크탑 무변경 (GTA 무게만 의도적 모바일 분기, 위 ⚠).

### 7-3. 나머지 페이지 (같은 방식)

Managed (표는 V8 바텀시트 적응 유지) → Pricing → Manifesto → Newsroom → Press Release → Changelog → GetTheApp

**각 페이지 착수 시 반드시 확인:**
- `.rows3` 같은 **가로 스와이프 스트립이 또 있는지** (`overflow-x:auto` / `scroll-snap-type` / `grid-auto-flow:column` 검색)
- `line-height:1.3` · `font-size:12px` · 사다리 밖 gap 값 (드리프트 패턴)
- Newsroom·Changelog·Press Release 는 **스트림 오프너(22) 와 항목 제목(17) 두 단이 다 필요**하다. 항목을 22 로 올리면 오프너 자리가 없어진다

### 7-4. 전 페이지 완료 후

Dave 리뷰 → 플래그 2 건 결정 (NO.02 밴드 타이틀 2줄 카피 / 푸터 아코디언 컴플라이언스) → 에셋 V9 자체 복사 → Figma 동기화

---

## 8. 함정 모음

1. **`.rv` 진입 애니메이션 (translateY 14px) 이 실측을 ±14 왜곡** → 측정 전 전 요소에 `.in` 부여 + **700ms 대기**
2. **V8 `.band>*` linearizer 가 `!important`** → 오버라이드도 `!important`
3. **V8 `.band{padding:72px 0}` 이 비-secpad 섹션(loop3 등)에 숨어 있어** 패딩이 이중으로 쌓임
4. **bash heredoc 안 CSS 유니코드 이스케이프가 깨짐** → 글리프는 문자 그대로 (`'−'`)
5. **인경 화면과 폭이 다르면 결론이 달라짐** → 375/610/850 삼중 검증, 캐시는 쿼리로 우회
6. **셀렉터 특정성** — V8 은 `.clog .ci .ct` 처럼 3단으로 쓰는 경우가 많다. `.clog .ct` 로 오버라이드하면 진다
7. **컴포넌트 컨테이너의 `font-weight` 를 측정하지 말 것** — `.bigh2` 처럼 실제 텍스트가 자식 span 인 경우 컨테이너 값은 렌더와 무관
8. **브라우저 페인 스크린샷이 JS 스크롤보다 한 박자 늦다** — 같은 화면이 두 번 나오면 한 번 더 찍거나 수치 검증으로 대체
9. **Claude Code 브라우저 페인의 `resize_window` 는 `resize`·`matchMedia change` 이벤트를 발생시키지 않는다** (CDP 뷰포트 오버라이드라 `matches` 값과 CSS 는 바뀌지만 이벤트가 안 온다). 리사이즈로 발동하는 JS 는 이 페인에서 **검증 불가** — 폭마다 새로 로드해 검증하고, 리사이즈 경로는 실제 브라우저에서 확인할 것

---

## 9. V10 검수 기록 (2026-08-16)

**0 error / 23 warning** — 경고는 전부 `.dr3-*` UI 리플리카 카드의 px 값이고 독트린 면제 대상. `landing page_ref/V9` 원본과 **경고 수·내용 동일** = 이번 작업이 만든 것이 아님.

| 항목 | 결과 |
|---|---|
| 파일 무결성 | NUL 0 · 끝 `</html>` · `<div>` 105/105, 133/133 · `<script>` 3/3, 5/5 |
| ref 대비 diff | **JS 게이트 블록 외 0** (콘텐츠 누락 없음) |
| 홈 375 / 610 / 850 | 화면 밖 요소 0 · 페이지 가로스크롤 0 · `.v9band` 3개 생성 |
| SD 375 / 610 / 850 | 동일 + `.rows3` 3행 전부 화면 안 (375 기준 20~355) · `scrollWidth == clientWidth` |
| 데스크탑 1440 스모크 | `.navmenu` flex · `.burger` none · `.step`/`.sys-legend` absolute · `.dr-doc` 노출 · `.rows3` grid 유지 · `.v9band` 0 |
| DS 값 실측 (375) | 모드 제목 17/19.55 · 스텝 제목 17 · 스텝 설명 13 · 가격 15/21 · 뉴스 헤드 17/19.55 · 범례 13/18.2 · SD hbar 28/30.8 (=1.1) · pay 리드 15/21 |
| 콘솔 에러 | 0 |
| 리사이즈 게이트 | 로드 경로 양방향 확인 (모바일 로드 → 적용 / 데스크탑 로드 → 미적용). **리사이즈 경로는 함정 9 때문에 이 환경에서 미검증** — 배선은 합성 `change` 이벤트로 확인 (matches:false→미실행, true→1회, 재발화→중복 없음) |

**추가 적용 (2026-08-16):** §7-1 밴드 이미지 높이 C(360) — 375/610/850 실측 + 1440 스모크 완료. 데스크탑 1440 에서 화면 밖 요소 5 개가 잡히지만 (`VIDEO` 3 · `IMG.ph1` · `IMG.ph2`, 전부 풀블리드 미디어) **ref 원본과 동일한 5 개**이고 페이지 가로 스크롤은 0 = 원래 설계, 이번 변경과 무관.

**§7-2 검수 (2026-08-16):**

| 항목 | 결과 |
|---|---|
| 섹션 하단 잔여 | 375 / 610 / 850 각 폭에서 `section.band` 9 개 전부 `padding-bottom` 과 오차 ≤1px. **불일치 0** |
| SD 1440 스모크 | `.v9band` 0 · navmenu flex · burger none · `.rows3` grid(row) · `.row` static · 가로 스크롤 1425/1425 · 콘솔 에러 0 |
| 링크 | index → SD 6 개 전부 `/V10/self-directed.html` 해석 확인 (`#research`·`#benchmark` 앵커 실재) · SD → 홈 3 개 `/V10/index.html` |

**미적용:** 없음. §7-3 (Managed) 부터가 다음 작업.
